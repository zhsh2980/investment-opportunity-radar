# 投资机会雷达 · Agent 约定

## Agent skills

### Issue tracker

本仓库的 issue 以本地 markdown 形式存于 `.scratch/<feature>/`（已 gitignore，不随公开仓库发布）。见 `docs/agents/issue-tracker.md`。

### Triage labels

五个 triage 角色使用中文标签（待评估 / 待补充 / AI可做 / 需人工 / 不做）。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：根目录 `CONTEXT.md`（术语表）+ `docs/adr/`（决策记录）。见 `docs/agents/domain.md`。
